import { BrexBudgets } from "./index";

describe("brex-budgets-typescript-sdk", () => {
    it("initialize client", async () => {
        const brexbudgets = new BrexBudgets({
        });
    });
});
